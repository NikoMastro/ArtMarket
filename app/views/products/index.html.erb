<!--The cards should be same sizes-->
<div class="container">
  <div class="product-cards row">
    <% if @products.empty? %>
      <p>No products matched your search</p>
    <% else %>
      <% @products.each do |product| %>
        <div class="col-3">
          <div class="card m-3 ms-3 product-card">
            <% if product.photos.attached? %>
              <%= cl_image_tag product.photos.first.key, crop: :fill %>
            <% else %>
              <%= image_tag 'default_product_img.jpg', alt: "default image for list"%>
            <% end %>
            <div class="card-produc">
              <%= link_to "Show this product", product_path(product), class: "btn btn-outline-info", role: "button" %>
              <div class="card-product-infos">
                <div>
                  <h3 class="card-title"><%= product.title %></h3>
                  <p><%= product.user.photo.attached? %></p>
                </div>
                <div>
                  <h3 class="card-product-cat"> <%= product.category %></h3>
                </div>
                <h3 class="card-product-pricing">DAY/¥<%= product.price %></h3>
                <% if product.user.photo.attached? %>
                  <%= cl_image_tag product.user.photo.key, class: "card-product-user avatar-bordered" %>
                <% else %>
                  <%= image_tag "default_user.jpg", alt: "text", class: "card-product-user avatar-bordered" %>
                <% end %>
                <%# <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-product-user avatar-bordered" /> %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
<!--<div>#= link_to "Delete",product_path(product),data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this product?" }</div>-->
